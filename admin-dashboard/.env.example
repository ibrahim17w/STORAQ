# Database (same PostgreSQL as the main app)
DATABASE_URL=postgresql://user:password@host:5432/storaq
PG_SSL=false

# Admin JWT secret (use a DIFFERENT value than main API)
ADMIN_JWT_SECRET=change-this-to-something-random-and-long

# Port
PORT=4400
